#include "main.h"

/**
 * _memset - returns the upper case of s
 * @s: pointer to string to be converted
 * @b: another set of characters
 * @n: number of chars
 * Return: empty char
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
