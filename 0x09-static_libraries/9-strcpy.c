#include "main.h"

/**
 * _strcpy - returns the upper case of s
 * @dest: pointer to string to be converted
 * @src: another set of characters
 * Return: empty char
 */

char *_strcpy(char *dest, char *src)
{
	return (dest);
}
