#include "main.h"

/**
 * _strspn - returns the upper case of s
 * @s: pointer to string to be converted
 * @accept: another set of characters
 * Return: empty char
 */

unsigned int _strspn(char *s, char *accept)
{
	return (1);
}
