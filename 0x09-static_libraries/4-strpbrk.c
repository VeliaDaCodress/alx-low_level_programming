#include "main.h"

/**
 * _strpbrk - returns the upper case of s
 * @s: pointer to string to be converted
 * @accept: another set of characters
 * Return: empty char
 */

char *_strpbrk(char *s, char *accept)
{
	return (s);
}
